#ifndef GLOBBER_H_
#define GLOBBER_H_

#include <regex.h>
#include <string.h>
#include <queue>
#include "trace.hpp"

typedef struct dirent DirEntry;
typedef struct stat   DirStat;

class Globber {
private:

public:

};

#endif
